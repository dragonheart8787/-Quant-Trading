# COLLAB.md — 多 Agent 協作規則

> 本專案可能由 Claude Code、Codex，或其他 coding agent 交替執行任務。
> 這份文件是給**任何**接手的 agent 看的第一份文件，目的是確保不同 agent
> 交接時不會遺漏既有紀律。

## 開始任何任務前，強制依序讀完這四份文件

1. `AGENTS.md` — 工作紀律、Gate 制、Definition of Done
2. `CLAUDE_2.md` — 架構、技術規範、canonical schema
3. `HANDOFF.md` — 現況快照，最後更新時間、目前卡在哪
4. 本檔 `COLLAB.md` — 協作規則本身

如果你是被要求「繼續上次的工作」，在讀完以上四份之後，**先做狀態盤點**，
不要假設 HANDOFF.md 記錄的內容跟實際程式碼狀態同步（兩者可能因為
上一個 agent 被中途中斷而不一致）：

- 跑一次完整測試套件，回報實際通過數字，跟 HANDOFF.md 記錄的數字對照
- 檢查 HANDOFF.md「最後更新」時間點附近提到的檔案，實際內容是否跟
  文件描述的完成度一致
- 如果程式碼與文件描述不一致，先回報差異，不要自行判斷「應該以哪個
  為準」就逕自繼續

## 不要動清單（跨 agent 共同遵守）

以下檔案除非使用者在當次任務中明確解除限制，任何 agent 都不得修改：
- `strategy/ma_rsi.py`
- `backtest/vector_engine.py`
- `backtest/event_engine.py`
- `risk/manager.py`（除非使用者明確在當次任務中解除，如放空風控任務時）

如果任務描述沒有明確提到解除某項限制，**視為限制依然有效**，即使
上一個 agent 在前一次對話中曾經被允許修改。解除限制是「當次任務範圍」，
不是永久生效。

## 交接時必須更新的文件（不能跳過）

任何 agent 完成一項任務、或任務被中斷前，都必須確保以下同步：
- `HANDOFF.md`：現況快照、下一步建議、已知問題
- `CLAUDE_2.md`：如果動到架構/schema/檔案結構
- `AGENTS.md`：如果這次任務產生了新的方法論教訓（比照既有格式）

如果任務因為任何原因（token 用盡、使用者中斷、錯誤）無法完成到
「更新文件」這一步，**下一個接手的 agent（不論是否同一個工具）**
有責任先做上述「狀態盤點」，補齊文件與程式碼的落差，才能繼續新任務。

## 測試是唯一的真相來源

不要以「HANDOFF.md 說已完成」或「上一個 agent 說測試通過」作為
判斷依據，任何接手時一律重新跑一次完整測試套件：

```bash
.venv\Scripts\python.exe -m pytest tests/ -q
```

如果數字跟 HANDOFF.md 記錄的不一致，先回報，不要假設是自己環境問題
就跳過。

## 風格差異的處理

不同 agent 可能有不同的預設輸出風格（例如交付摘要的格式、是否主動
使用表格、commit message 習慣）。這不強制統一，但以下事項**不可**
因為換了 agent 就改變：
- Gate 制流程（不可跳站，除非使用者明確同意並在文件記錄風險）
- Definition of Done 四項標準（可執行、有測試、有數據佐證、有交付摘要）
- 防未來函數規範（shift(1)、排他式 as-of join 等既有機制）
- 「不要動清單」

## 如果你不確定某個規則是否還適用

不要自己猜測或用「這應該是舊規則了」為由略過。直接在回報中列出
你的疑慮，讓使用者確認，再繼續。這比自行判斷後執行、事後才發現
理解錯誤的修復成本低得多。
