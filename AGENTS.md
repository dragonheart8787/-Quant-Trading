# AGENTS.md — 代理人工作規則

> 本文件定義 Claude Code 在此 repo 中「怎麼工作」。專案架構與技術知識請看 `CLAUDE_2.md`，本文件只規範流程、紀律與 Definition of Done。

## 工作前必讀

開始任何任務前，先讀 `CLAUDE_2.md`。如果改動會影響架構或目錄結構，完成後同步更新 `CLAUDE_2.md`，不要讓文件與程式碼漂移。

## Gate 流程（不可跳站）——Phase 編號唯一正式定義

> 本節是全專案 Phase 編號的**唯一正式定義**（2026-07-07 統一為七站制，使用者確認）。
> `CLAUDE_2.md` 的「市場成熟度檢核表」（研究／驗證／模擬／上線）是與 Phase 正交的
> 另一維度：每個市場各走一遍的成熟度檢核，**不編號、不稱 Phase**，映射見該節。

| Phase | 內容 | 狀態 |
|---|---|---|
| 1 | 垂直切片（資料→訊號→向量回測→walk-forward→風控→放空） | ✅ 完成 |
| 2 | 事件驅動引擎驗證（event_engine E0/E1/E2、兩層驗收、Δ歸因） | ✅ 完成（2026-07-07 結案，DoD 四項通過） |
| 3 | 拓寬市場 adapter（台股 TWSE/FinMind 籌碼、美股；新資料源一律走 provider adapter） | ← 下一站 |
| 4 | ML 訊號層 | |
| 5 | RL 倉位層（FinRL 屬此站；跳過 Phase 4 直接做需走跳站程序） | |
| 6 | paper trading + 監控 | |
| 7 | 限額實盤 | |

未完成前一個 Phase 的 Definition of Done，不得開始下一個 Phase 的程式碼。如果使用者要求跳站（例如「直接上 RL」「直接接實盤」），先提出風險提醒，並確認使用者明確同意後才繼續。

> **編號沿革（讀舊對話/舊筆記時注意）**：2026-07-07 前的舊版把 Phase 2 定為「拓寬
> 市場」，但實際工作用語（HANDOFF、run_phase2_event.py）的 Phase 2 = 事件驅動引擎，
> 統一時採既成用法，拓寬市場起各站 +1。舊編號的 Phase 2/3/4/5/6（拓寬市場/ML/RL/
> paper/實盤）= 新編號的 Phase 3/4/5/6/7。

## 每個 Phase 的 Definition of Done

一個 Phase 算完成，必須同時滿足：

1. **可執行**：程式碼能跑，不是「能 import 就算了」。
2. **有測試**：風控、回測核心邏輯（防未來函數、停損停利、熔斷）必須有對應 `tests/` 單元測試，且全部通過。
3. **有數據佐證**：用真實或至少 mock 資料跑過一次完整流程，並回報具體輸出（報表數字、log、或截圖），不是空殼函式。
4. **有交付摘要**：完成後列出新增/修改的檔案清單，以及下一步建議——但不要未經確認就自動做下一步。

## 程式風格慣例

- Python：型別標註（type hints）、`dataclass` 用於結構化資料、函式單一職責。
- 禁止 `DataFrame.apply(axis=1)` 做向量化計算可解決的事；優先用 `rolling`/`ewm`/NumPy 向量運算。
- 所有時間序列驗證用 `TimeSeriesSplit` 或 walk-forward，禁止隨機 K-fold。
- 所有回測訊號必須 `shift(1)` 後才轉成持倉，避免未來函數。
- commit message 用簡短中文或英文描述「做了什麼」，不要寫「fix」「update」這種無資訊量訊息。

## 禁止事項（沒有例外）

- 不寫死 API key、密碼、憑證路徑在程式碼或 commit 紀錄裡，一律用環境變數（`config/settings.py` 讀取）。
- 不在沒有風控模組與對應測試的情況下實作任何「真實下單」程式碼路徑（`broker/live_*.py` 的下單方法可以先寫，但呼叫它的上層邏輯必須經過 `risk/manager.py` 才能執行）。
- 不在使用者未明確要求或確認的情況下：串接真實實盤帳戶下單、修改風控的熔斷/槓桿上限數值、安裝會動到全域環境的套件。
- 不用單次回測或單次 RL 訓練結果下結論；模型/策略效果的陳述需基於多 seed 或 walk-forward 多 fold 的結果。

## 方法論教訓（已驗證，勿重蹈）

### 訊號時間特性互斥（2026-06-25）
**教訓**：滯後確認型指標（regime_window=120 rolling）與即時反轉型指標（RSI 超買/超賣）
的時間窗不重疊，組合條件會導致永遠不觸發。與「根因是 regime 不是參數」（結論 2）同類型：
問題是結構性訊號設計，不是參數調整能解決的。

實例：`trend_down（120根滯後）AND RSI >= 70（即時反轉）` → fold4 n_short=0。
修正：換成「不深度超賣（RSI > 30）」代替「超買」，與滯後確認型指標時間窗相容。

**應用準則**：
- 新增多條件組合訊號前，先問「各條件的時間特性是否相容？」
- 滯後型指標（regime、長期均線）配合「狀態判斷」（RSI 不在極端區），而非「觸發點」（RSI 超買/超賣）

### 多尺度 regime 一致性過濾的兩面性（2026-07-03）

**教訓一（問題面）**：局部 regime 誤判不限單一 fold，range regime 內同樣可能出現假性
trend_down 子段，多尺度一致性檢查需作為空頭觸發的通用門檻。

**教訓二（代價面，A2 實驗實證）**：長視窗過濾不是免費的，付出兩種滯後成本：
1. **誤殺趨勢起點**：下跌剛開始時長視窗尚未確認，最有利可圖的早期空單被濾掉
   （上次 fold4 時間窗：130 空bar 被濾 41 根，空頭相對 long-only 的優勢從 +27.9pp 縮到 +3.3pp）。
2. **殘留記憶**：長視窗涵蓋前一段下跌時，後續 range/反轉段仍判 trend_down，
   過濾效果打折且留下的是「趨勢尾端、接近反轉」的空單
   （上次 fold5 時間窗只濾掉 20%；本次 fold5 留下的空單反而虧更多）。

**應用準則**：
- 評估過濾器必須同時報告「濾掉的假訊號」與「誤殺的真訊號」，不可假設加過濾就是好事。
- 任何 regime 視窗參數（120/300）與 ATR N 倍數同屬「待歷史資料校正」，
  單一 4 個月資料集的結果不足以定案。（後續狀態：A2/300 已於 2026-07-06
  定案不採用、不再校正；120 已驗證不縮短；ATR N/window 校正 2026-07-13
  完成——三零重疊窗×15-fold 兩層驗證，建議維持現值，等使用者拍板。）
- 驗證用資料未落地導致 fold 邊界隨抓取時間漂移，跨 session 驗收同一個 fold
  需重建時間窗近似，結論帶誤差（2026-07-03 已補 `data/storage_sqlite.py` 落地解決；
  實證：僅 3 小時漂移就翻轉了「A2 是否降低跨 fold std」的彙總結論——
  fold 對照一律使用凍結資料集，勿用即時抓取數字下結論）。

### 彙總統計的檢定力邊界（2026-07-03，5-fold 與 15-fold 兩層實證）

**教訓**：5-fold/單 symbol 的彙總統計對細緻訊號品質判斷（如 A2 vs v2）檢定力不足，
3 小時資料位移即可翻轉結論；擴大資料量/fold 數應優先於繼續微調訊號參數。

**15-fold 擴大驗證後的補充（8000 根 × 兩個錯位 6.5 週的時間窗）**：
- A2-v2 的彙總差異在兩窗間仍翻轉符號（窗A +9.0pp / 窗B -0.4pp），但兩窗一致支持
  「A2 ≈ v2，差異落在雜訊內」這個層級的結論——fold 數變多讓「無法區分」本身變穩定。
- 同 regime 分組後，fold 間仍大幅擺盪（組內 std 24~130pp 年化）：**1h 尺度上這個
  策略族的訊號雜訊比本來就高**，這是重要結論，不是失敗。
- 唯一跨窗符號穩定的模式：**trend_up fold 中 A2-v2 恆 ≥ 0**（長視窗把上漲段的
  逆勢空單全數濾除）；range fold 的 A2 效果純屬雜訊（兩窗符號相反）。
  **（2026-07-06 推翻：見下方補充，勿再引用「恆 ≥ 0」）**
- 應用準則：訊號品質的細緻比較（幾 pp 級）需要 regime 條件化 + 多時間窗符號
  一致性檢查；只看單一資料集的彙總 mean/std 排序必然被雜訊主導。

**獨立時間窗驗證後的補充（2026-07-06，窗C/D 完全零重疊）**：
- **錯位但重疊的時間窗不是獨立樣本**：窗A/B 重疊 86.2%，5 個 trend_up fold
  實際只有 3 個獨立時間段（A9↔B7、A14↔B12 各重疊 80%，同段被雙計）。
  跨窗一致性檢查若要當獨立證據，時間範圍必須互斥；重疊窗只能檢驗
  「fold 邊界敏感度」，表面 fold 數會高估檢定力。
- **「trend_up 中 A2-v2 恆 ≥ 0」在獨立樣本上被反例推翻**：新增 8 個獨立
  trend_up fold（窗C/D 各 4）中 7 個 ≥ 0，但窗C fold9（2024-02~03 破前高段）
  A2-v2 = **-44.5pp**——v2 空單抓到急漲中的深回檔反而賺錢，被長視窗全濾。
  「上漲/盤整大勢中的假性 trend_down 空單同時含賺錢與虧錢子段，長視窗過濾
  無法區分」在 trend_up 段同樣成立（與 range fold3 教訓同型）。
- 據此**條件化A2（只在 trend_up 擋空單）未實作即否決**——建立在「恆 ≥ 0」上的
  設計，其前提在第一批真正獨立的樣本就出現大反例。
- 應用準則：「恆成立」等級的模式主張，需在**完全不重疊**的時間窗上驗證；
  n 個重疊窗的一致性 ≠ n 份證據。條件化設計上線前，先問前提模式的
  獨立樣本數有幾個。

### 未完成 K 線與資料凍結（2026-07-06）

**教訓**：抓取當下的最後一根 K 線可能是**尚未完結的 candle**；之後任何重新抓取
＋冪等 upsert 都會把它替換成完整版——「upsert 冪等」保證不重複列，**不保證數值
不變**。實例：乾淨基準表最初版本的 fold5 與現行 DB 差 ~0.5pp（僅窗尾一根 close
不同，年化放大），fold1-4 逐位不變。

**應用準則**：
- 落地/凍結資料時，排除或事後補抓「進行中」的最後一根；宣稱「逐位重現」前
  先確認窗尾 K 線已完結。
- 發現重現偏差時，先檢查「只有含最後一根的 fold 變了嗎」——是的話即此症狀，
  不是引擎或訊號的 bug。

### 向量化回測的再平衡假設偏差（2026-07-06，事件引擎 E0 實證）

**教訓**：vector_engine 的 `position=-1` 在數學上是「逐根再平衡的反向曝險」
（∏(1-r)），不是固定數量空單（2 − P/P_entry）。兩者差異路徑相依：單向趨勢中
再平衡佔優（凸性），**震盪路徑中再平衡有波動拖累、低估固定倉位空單的損益**。
實證：乾淨基準 fold4（震盪下跌）v2 向量 +67.3% vs 事件引擎固定倉位 +85.7%
（差 +18.4pp、佔 27%；A2 同向 +19.5pp）。多頭無此問題（兩模型等價，E0 全 fold 0.0pp）。

**應用準則**：引用向量版**空頭**報酬數字時必須註明近似性質——偏差可達雙位數
pp、方向不定；精確數字以事件引擎（backtest/event_engine.py）為準。

**補充（2026-07-06，E2 全 fold 歸因實證）**：向量 vs E2 的差距**主成分是曝險
倍數**（向量隱含空頭 1.0x vs 風控規則二 0.5x；平均|Δ|≈22pp，10 案例中 8 個
為最大成分），倉位模型路徑差 7~11pp 次之，熔斷本批 0（未觸發情境≠機制無效，
只是市況沒踩到門檻）。**策略／過濾器價值評估一律以 E2 數字為準**；向量引擎
保留作訊號層快速迭代（同曝險假設下的相對比較）。

**補充二（2026-07-13，規則一 sizing E2 實證——同類教訓的延伸）**：sizing 公式
**本身**的曝險縮放，可以在**風控完全沒有介入**（強平 0 觸發、觸發 bar 集合
與對照組逐一相同）的情況下大幅改變報酬。實證：乾淨基準 fold4，規則一
（risk_per_trade，ATR 呼吸倉位）vs 槓桿上限全倉，兩者強平皆 0 次，年化
+27.5% → -1.7%——全來自高波動下跌段 ATR 大 → 空頭曝險 0.5x 縮到平均
0.15x，賺錢空單的參與度被縮倉吃掉。這不是風控「介入」造成的差異，是
sizing「設計」本身造成的差異；「固定風險預算」與「高波動趨勢段獲利參與度」
是本質權衡，不是 bug。

**應用準則（兩補充合併）**：
- 比較任何兩種 sizing／風控配置時，先驗證「觸發 bar 集合」機制層是否等同，
  再把報酬差異歸因到曝險設計——不可把 sizing 造成的參與度變化誤讀為
  風控誤殺或訊號品質差異。
- leverage_cap（固定曝險）vs risk_per_trade（呼吸曝險）是**風控哲學選擇**，
  不是「誰數字好就切換」的技術問題（2026-07-13 使用者定案：E2 正式基準
  維持 leverage_cap；r1 完整實作與回歸驗證保留，日後要做哲學選擇時直接
  用現成路徑跑對照，不需重新實作）。

### 新增預設參數的隱藏影響範圍（2026-07-15，滑價基準切換實證）

**教訓**：這與「文件與程式碼不同步」（COLLAB.md 既有紀律）性質不同，是
**「參數預設值變更的隱藏影響範圍」**這個新類別——任何呼叫端原本靠「省略
某參數」來表達一個特定假設（如零摩擦、關閉某機制），一旦該參數新增或其
預設值改變，所有靠省略表達假設的舊呼叫端都會**靜默**改變行為，不會報錯、
不會警告，只會在數字對不上時才被發現。

實證：`event_engine.py` 新增 `slippage_bps`（初始預設 0.0，逐位不變）時，
所有既有呼叫（測試與 runner）自然靠省略拿到零摩擦——這在當下是正確的
（新增參數本身就承諾預設值不變）。但**下一步**當使用者拍板把 `slippage_bps`
的預設值從 0.0 改為 2.0（正式切換基準，不是新增選配）時，同一批「省略此
參數」的呼叫端全部**靜默**從零摩擦變成 2bp 摩擦：
- 18 個單元測試斷言的精確成交價/數量/權益數字全部基於隱含零滑價，預設值
  一變全部失效（跨 4 個檔案，涵蓋 Phase 2 核心引擎、E2 風控整合、規則一
  sizing、multiplier 校正四個不同輪次寫的測試）。
- 五支已結案 runner（`run_atr_calibration.py`、`run_rule1_sizing.py`、
  `run_multiplier_calibration.py`、`run_phase2_event.py`、`run_ml_signal.py`）
  若照舊重跑，會**靜默**產生與 HANDOFF 記錄不符的新數字，違反「固定資料集
  原則」的逐位重現保證——如果沒有主動盤點，這種偏差可能要到下次重跑對照
  才會被發現，且不會有任何錯誤訊息提示原因。

**應用準則**：
- 新增一個帶預設值的參數時，若該參數未來**有可能**改變預設值（不是永久
  的「開關」，而是「目前的估計值」），評估時就該想到：所有靠省略拿到
  當前行為的呼叫端，日後都要嘛承受靜默行為改變，要嘛需要逐一明確 pin。
- **一旦真的要改預設值（不是新增參數本身），先跑一次全套測試量測實際
  失敗範圍，不要用推論猜測**——測試失敗的具體檔案與數量才是準確的影響
  範圍，比"應該只影響 X"的推論可靠。
- 已結案輪次（HANDOFF 標「★結案★」的章節）依賴的 runner，若其呼叫端
  隱含依賴即將變動的預設值，**必須明確 pin 舊值**，保住歷史數字的逐位
  可重現性；同時在 HANDOFF 對應章節加註「數字基準 = 舊值（歷史快照）」，
  避免日後引用時誤認為用了新基準。
- 這個模式與具體是哪個參數無關，**任何日後新增預設參數都可能重演**，
  評估影響範圍時應主動检查「有多少呼叫端在依賴省略」，不要假設「只是
  加個新參數，舊行為不變」就等於「舊行為永遠不變」。

### 日內熔斷與 sizing 路徑依賴的交互作用（2026-07-15，ETH 多 symbol 驗證實證）

**教訓**：multiplier 校正輪（2026-07-14）的事前預期 P1「觸發 bar 集合跨
`short_risk_multiplier`（m）不變」的理由是「`check_forced_liquidation`
只看 quantity 正負號，與倉位大小無關」——**這個理由本身沒錯，但只涵蓋
了強平判定函式本身，沒有涵蓋上游是否有部位可供判定**。日內熔斷
（`RiskManager.update_equity` 的 `daily_pnl_pct ≤ -3%` 判定）比較的是
**當日累積權益**，而空頭部位的權益路徑隨 m 縮放（quantity 影響已實現/
未實現損益、進而影響權益、進而影響隔天的熔斷門檻判定）——不同 m 值的
權益軌跡在某些日子可能剛好落在 -3% 門檻的兩側，導致**同一批進場單在
某些 m 下被 `rejected_circuit_breaker` 擋下、在其他 m 下被核准**，兩種
情況後續是否存在部位（進而是否可能觸發強平）就此分岔。

實證：ETH 多 symbol 驗證輪，60 個 fold×窗組合中 1 個（窗D fold9）出現
m=1.0 的空頭進場未被熔斷擋下（其餘 m=0.25/0.5/0.75 被擋），導致 m=1.0
多出一次強平（liq_bars 從 `[145]` 變成 `[145, 413]`）。BTC 資料域內 60
個組合從未踩到這個邊界（可能與 BTC 相對波動較低有關），ETH 較高的相對
波動度使這個一直存在但罕見的情境被踩中一次。

**應用準則**：
- 「sizing 參數不影響觸發機制」這類事前預期，如果理由只分析了**判定
  函式本身**（如 `check_forced_liquidation`），沒有涵蓋**上游會不會
  先被其他獨立機制（如熔斷）擋單**，表述就不完整——完整版本應該是
  「若無熔斷介入，觸發 bar 集合跨 m 不變」，不是「恆不變」。
- 這類邊界情境的發生率可能極低（本例 60 分之 1），**在原始驗證資料集
  上沒踩到不代表機制不存在**——換一個波動特性不同的資產（甚至只是換
  一段更長/更多的歷史）就可能踩到。日後看到「機制層不變量在新資料上
  出現少量偏差」，先查是否為這類「獨立機制交互作用」的已知模式，而非
  預設是 sizing 公式本身的 bug（本例 P2「未封頂損失/預算比率跨 m
  不變」逐位成立，已排除公式本身非線性）。
- 判斷「機制是否退化」時要看**發生率與根因**，不能只看「是否 100%
  吻合」——1/60 且根因明確的邊界情境，與大範圍系統性偏差是本質不同的
  兩件事，不應被同等對待或同等稀釋報告的清晰度。

**延伸意義（與「regime 決定勝負不是參數」「RSI 與 regime 時間錯位」同一類
教訓）**：任何跨參數的機制層不變量，驗證範圍僅限於已測試過的資料域；
擴大資料域（更高波動度、新資產、新市況）時，不能預設先前驗證過的不變量
依然成立，需要重新檢查——這次 ETH 踩到的 P1 分岔就是一個實例。

### 跨市場/跨頻率沿用風控參數前，需檢查目標市場子群體的波動特性分布（2026-07-16，台股 regime/ATR 校正輪實證）

**教訓**：BTC 1h 校正出的 ATR N=3 沿用到台股日K，聚合層面看起來「觸發
密度合理、fixed 線觸發次數少」，但按觸發時點的動態線水位分組後三組幾乎
不重疊（atr-only median 8.08% vs both 15.44% vs fixed-only 22.87%），再
按股票分層看全歷史 bar 分布，發現**同一組參數在不同波動特性的子群體上
是兩種完全不同的機制**：對大型權值股（2330）動態線 ≥15% 僅 0.23% bar、
設計意圖完好；對 TPEx 小型股（8069）有 19.52% 的交易日動態線在 15% 固定
網之上——「動態線是更敏感的第一層」這個設計前提對小型股**長期性不成立**，
且真實危機期（2008 金融海嘯、2015 全球股災）固定網反而先觸發、兩層防線
角色徹底反轉。根因是結構性的：日K 一根吸收整天波動，小型股日 ATR/價格比
長期 3~5%，N=3 自然坐在 10~15% 區間——與哪次事件無關。

**應用準則**：
- 跨市場/跨頻率沿用任何風控參數前，先檢查目標市場的**波動特性分布**
  （如 N×ATR/price 的全歷史分布），且必須對波動特性顯著不同的**子群體
  分開檢查**（大型股 vs 小型股、上市 vs 上櫃）——聚合數字（「密度 37.9%
  合理」「fixed 只有 3 次」）會把子群體的結構性失效稀釋掉。
- 「參數在 A 市場驗證過」只證明「參數適配 A 市場的波動尺度」，頻率轉換
  （1h→1d）會同時改變 bar 的波動吸收量，參數的經濟意義隨之改變，bar 數
  不變不代表語意不變。
- 診斷出結構性失效不等於馬上要改參數——若該機制在目標市場尚未被實際
  使用（如本例 ATR 強平在純多頭基準是休眠的），正確的參數（甚至架構，
  如要不要分層）屬於未來啟用該機制時的設計決策，提前校正是在沒有策略
  上下文的情況下優化，時機不對。先完整記錄發現，掛帳給對的任務。

### 校正統計量必須對準機制要守的分位，不是分布的中心（2026-07-16，分層 N 第一版 FAIL 實證）

**教訓**：ATR 分層 N 第一版用 trailing **median**(ATR/close) 錨定動態線
「典型水位」在 9%，事前預期小型股的「線 ≥15% 固定網」bar 佔比塌縮到近 0
——實際 FAIL（6446 幾乎沒改善、2603/2330 反而惡化）。根因不是實作 bug
（純函式單元測試全過、因果性無誤），是**錨定統計量選錯**：median 回答
「平常日子線在哪」，但強平防線機制要守的是「**極端日子**線會不會跑到
固定網之上」——median 錨 9% 數學上等價於「任何波動超過 trailing median
1.67 倍（15/9）的 bar 都越線」，而波動叢聚是市場本性，實測 test 窗 q95
相對 train median 的倍率 median 就有 1.46~1.79 倍。修正 = 換錨 trailing
**q95** 於 12%，把「連 95 分位波動日的線都留在網下」直接寫進公式——
常態 fold（44/60）塌縮達成（8069 17.65%→0.86%、6446 11.72%→0.68%）。

**應用準則**：
- 設計任何「校正參數使某條線/門檻不被越過」的機制時，先問**這條線要在
  哪個分位的情境下守住**——守極端就錨極端分位（q95/q99），守典型才錨
  median。錨錯分位的校正在單元測試層面完全正確、卻在真實分布上結構性
  失效，且失效方式是「測試全綠但目標沒達成」，只有事前判準能攔住。
- 這與「RSI 超買（即時反轉）配 regime 確認（滯後）時間窗不重疊」是同型
  教訓的統計量版本：**工具回答的問題**與**機制要解的問題**必須對齊，
  組合前先明說各自回答什麼。
- trailing 校正的結構性殘留（train 段之後才發生的劇烈 regime 突變，如
  2603 fold9 q95 位移 2 倍）**記錄不消除**，與漲跌停鎖死尾部風險同一
  處理態度；事前判準本身也可以（由使用者拍板）重新定義為「排除已知
  結構性限制後的可達成目標」，而不是硬凹原判準或靜默放寬。

### 使用未來已公告資訊的合法性分類（2026-07-16 建立，兩個先例後的通用標準）

**背景**：專案至今遇到兩次「表面上違反防未來函數紀律、實際上合法」的
非因果例外——①還原股價（用未來除權息事件回填過去價格水位）；②強制回補
日曆（bar t 決策讀 t+1 的停券旗標）。兩次都各自論證過，本節把判斷標準
固定下來，第三次遇到同類情況直接套用、不用重新論證。

**合法的非因果使用，必須同時滿足三個條件**：
1. **資訊在決策時點真實可得**：該資訊在回測模擬的決策當下，於真實世界
   已經公開（停券日程數週前公告、除權息比例事前公告）——不是「事後才
   知道」的資訊。驗證方式：找得到事前公告的機制證據（如停券日曆 51/52
   筆與除權息日交叉驗證確認公告制）。
2. **資訊不是價格/成交量的衍生物**：日曆日期、公司行動比例屬制度性
   資訊；任何從未來價格算出來的東西（未來報酬、未來波動、未來成交量）
   永遠不合法，無論包裝成什麼形式。
3. **用途區分「重新表述」與「決策輸入」**：還原價是對歷史價格水位的
   事後重新表述（訊號在還原價序列上仍走 shift(1)，因果紀律不變）；
   日曆旗標是決策輸入但滿足條件 1。兩類都合法但機制不同，文件要寫清楚
   屬於哪類。

**不合法的典型樣態（對照組）**：用未來 close 生成當根訊號（test_lookahead
_bias.py 攔的就是這個）；用「事後才確定」的資訊當作「事前已知」（如財報
「所屬期間」與「公告日期」的混淆——財報數字屬於 Q1 不代表 Q1 結束當天
就能拿到）；把逐位重現用的凍結快照當成決策當下可得的資訊。

**執行要求**：每個合法非因果例外都必須（a）在模組 docstring 明確標注
「這是刻意設計不是未來函數 bug」＋合法性論證；（b）寫一個文件性斷言
測試釘住行為，防止未來 session 誤判為 bug 而「修正」掉（先例：
test_adjustment_intentionally_uses_future_dividend_events、
test_calendar_flags_are_preannounced_information_not_lookahead）。

### 可重現性的邊界必須涵蓋執行環境本身（2026-08-15，雲端環境驗證輪）

**教訓**：專案已把「凍結資料集」做到很徹底（DB 落地、校驗和、fold 時間窗、
runner 逐位重現），但**執行環境本身一直沒被凍結**——`requirements.txt` 用
`>=` 鬆散範圍，等於宣告「任何未來版本都可以」。這是同一條可重現性鏈上唯一
沒鎖的環節：凍結了資料、凍結了參數，卻讓算資料的那層自由浮動。

實證有二：①`requirements.txt` **漏列 `scikit-learn`** 而長期沒被發現，因為
本機 venv 早就裝好了——「本機能跑」把缺漏遮住，直到第一次真正的乾淨安裝
（雲端 session）才暴露，7 個測試模組直接 collection error。②專案先前已經
被套件版本咬過一次：pandas 3.0 的 `to_datetime` 把 ISO 字串解析成
`datetime64[us]`，與 storage int64 奈秒往返的 `[ns]` 不符 → `merge_asof`
MergeError（features.py 的 ns/us bug）。這與「新增預設參數的隱藏影響範圍」
同型：**不報錯、不警告，只在數字對不上時才被發現**，差別只在變動來源是
第三方套件、不是自己的程式碼。

**應用準則**：
- 相依一律 `==` pin 死，**含遞移相依**（只 pin 直接相依，乾淨安裝仍會抓到
  新版的 numpy/scipy）。pin 的基準必須是「實際跑過全套測試通過」的那組版本，
  不是憑印象寫的版本號。
- 跨環境的 pin 衝突（如 numpy 2.4.6 無 cp314 wheel、2.5.x 不支援 3.11）
  **不可用放寬成範圍來解決**——正確做法是各環境各自 pin，再用凍結資料集跑批
  做逐位對照證明兩者等價。放寬範圍等於把「已驗證等價」降級回「假設等價」。
- **「本機能跑」不等於「乾淨環境能跑」**：宣稱環境相依完整前，跑一次真正的
  乾淨安裝，不要用既有 venv 驗證既有 venv。

### 環境層的宣稱要用「反證設計」驗，不要用讀碼推論（2026-08-15，同輪）

**教訓**：驗證「全套測試不需要 API token」時，讀程式碼確認測試都有 mock
是**弱證據**——只證明「我看到的那幾個檔案有 mock」，不排除某個沒細看的
測試在特定條件下走真實網路。正確做法是**設計一個「若宣稱為假則必然爆炸」
的環境**：用 sitecustomize 把 `socket.connect`/`getaddrinfo` 全部換成拋例外，
再跑全套 → 379 passed 就是強證據，任何即時打 API 的測試都不可能靜默通過。

同型手法在同輪的第二個應用：驗證「Python 版本差異無影響」不是比對版本號
或讀 changelog，而是在兩個版本各跑一次凍結快照 runner 再 `diff`——輸出
**逐位元相同**才是結論，這與專案既有的「byte-diff 驗證預設路徑逐位不變」
是同一個判準，只是把它從程式碼變更移用到環境變更上。

**應用準則**：
- 環境層/機制層的「不影響」宣稱，優先找**可證偽的量測**，不要停在
  「看起來都有 mock」「版本號差不多」這種讀碼推論。
- 已知殘留限制要明說，不要用強於證據的措辭：本輪實際驗到的是 3.14.0rc2，
  不是本機的 3.14.6，所以正確表述是「3.14 系列與 3.11.15 在本專案凍結
  資料集上逐位相同」，不是「3.14.6 逐位相同」。

## 風控規則（已實作，2026-06-25 起）

### 空頭風控規則（SHORT_SELLING_TASK.md 定義）

1. **規則一：空頭單筆風險比例**
   - `RiskConfig.short_risk_multiplier`（預設 0.5）控制空頭風險係數
   - `risk_per_trade_short = risk_per_trade * short_risk_multiplier`
   - `position_size(side="short")` 使用 `risk_per_trade_short`
   - 2026-07-13 起可經 event_engine `sizing_mode="risk_per_trade"` 在 E2 生效
     （停損距離 = forced_liq_atr_n × 決策bar ATR，只接空頭；預設模式仍為
     槓桿上限全倉、行為不變），詳見 HANDOFF「規則一 sizing 接入 E2」節

2. **規則二：多空槓桿上限分開設定**
   - `max_long_leverage`（向後相容 `max_leverage`）與 `max_short_leverage = max_long_leverage * 0.5`
   - `max_gross_leverage` 預留欄位（目前多空互斥，等同單方向上限）
   - `approve_order(side, current_position_qty)` 執行多空互斥檢查
   - 已有多頭不可開空頭（反之亦然）→ `REJECTED_CONFLICTING_POSITION`

3. **規則三：空頭強制平倉（獨立於日內熔斷）**
   - `check_forced_liquidation(position_quantity, avg_price, current_price, atr_value) -> bool`
   - 雙層防線取較嚴格者：ATR 動態防線（N=3, window=14；N 與安全網比例已參數化為
     `RiskConfig.forced_liq_atr_n` / `forced_liq_safety_pct`，2026-07-13 歷史校正
     完成、建議維持現值，詳見 HANDOFF.md「ATR 強平防線校正」節）+ 15% 固定安全網
   - 此函式與 `is_circuit_broken`（帳戶整體日內熔斷）完全獨立

## 溝通方式

- 任務開始前，如果需求模糊（例如「優化策略」沒講優化什麼指標），先問清楚，不要自行假設後硬做。
- 任務完成後，用條列式摘要回報：做了什麼 / 測試結果 / 數據結果 / 建議下一步。不要長篇敘述式報告。
- 遇到 API 限制、套件版本衝突、資料缺失等阻塞問題，直接回報問題與已嘗試的排查方式，不要悶著頭重試到天荒地老。
